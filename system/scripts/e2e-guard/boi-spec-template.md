### e2e-verification (auto-appended guard)
PENDING

**Spec:** Run the E2E verification guard against the deployed service.
**Verify:** `python3 .hex/scripts/e2e-guard/verify.py --url <URL> --selectors <SELECTORS> && echo PASS`
