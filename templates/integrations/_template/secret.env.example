# REPLACE_ME secrets schema — committed placeholder values
# Actual values live in .hex/secrets/REPLACE_ME.env (chmod 600, gitignored)
# Copy this file to .hex/secrets/REPLACE_ME.env and fill in real values.

# Required
REPLACE_ME_API_KEY=<api-key-from-provider-dashboard>

# Optional
# REPLACE_ME_ENV=prod
